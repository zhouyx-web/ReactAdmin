import React, {Component} from 'react'
import {
    Form
} from 'antd'

const {Item} = Form

export default class AuthForm extends Component {
    render() {
        return (
            <Form>
                
            </Form>
        )
    }
}